# 5.1 AI API Layer：wire-protocol streaming 与 lazy loading

> **⚠️ v0.84.x**：① `ModelsStreamTransforms` 改名 `ModelsRequestTransforms`（header 变换作用于所有已认证 provider 请求）；② Mistral 从 SDK transport 换成**原生 Chat Completions HTTP stream**（删掉 generated client 和 schema runtime 开销，`package.json` 依赖里已无 `@mistralai/sdk`）——"每个 API 一个 adapter"的叙述更成立了；③ 新增 deferred provider request contracts（durable response handles、authenticated fetch/cancel dispatch、faux-provider）；④ OpenAI Responses deferred tool loading 支持 message-anchored `additional_tools`（`Model.compat.supportsAdditionalTools` 开关）；⑤ `StreamOptions.samplingParams` 透传任意 OpenAI-compatible 采样参数（仅 completions/responses/Azure responses 三个 adapter 应用）。
>
> 2026-08-17 已对照 v0.84.2 复核。
>
> **⚠️ v0.84.3**（2026-08-25 核对）：① `SimpleStreamOptions` 新增 **`toolChoice?: "auto" | "none"`**（types.ts:82），被**所有** `streamSimple` 转发（completions/anthropic/bedrock/google/vertex/mistral/responses/codex/azure/pi-messages）；② `ThinkingTokenBudgetField` 泛化原 `supportsThinkingTokenBudget`（后者变成 alias，types.ts:97/605-607），新增 `$var: "thinking.budget"` chat-template kwarg，预算计算抽出为 `DEFAULT_THINKING_BUDGETS`/`thinkingBudgetForLevel`/`clampThinkingBudgetToAnswerRoom`（simple-options.ts:57-91）；③ OpenAI Completions 支持 reasoning details replay（`reasoning.summary/.text/.encrypted` 累积进 `thinkingSignature` 并重发 `reasoning_details`，openai-completions.ts:625-637/1232-1277）；④ Anthropic server-side fallback beta（`server-side-fallback-2026-07-01`，`allowedFallbackModels` 发 `params.fallbacks`，anthropic-messages.ts:177/1104-1110）；⑤ Bedrock 非 Anthropic 模型的 redacted reasoning 往返（`reasoningContent.redactedContent` base64 进 `thinkingSignature`）+ 原始响应头中间件；⑥ 大多数 adapter 默认加 pi User-Agent。
>
> **⚠️ v0.84.4**（2026-09-01 核对）：① **tool_choice 语义定稿**：#8607 曾给 openai-completions 加"无 tools 就省略 `tool_choice`"守卫，同版本内被 6b36eb592 **回退**——最终态是 ai 层**无条件透传** `options.toolChoice`（openai-completions.ts:851，v0.84.3 时 ：813），"无 tools 就别传"的责任移到调用方（compaction 已移除显式 toolChoice，见 [3.1](../03-Memory/3.1_Compaction.md)）。② **reasoning details 拼接 + 一次序列化**（#8605/#8671）：相邻同类型 text/summary delta 合并（`appendOpenAIReasoningDetail`，:259）；`thinkingSignature` 不再每 delta `JSON.stringify`（O(n²)），累积到流作用域变量，`thinking_end`（:431）或流收尾（:695）时一次写入——③ 条描述的"累积进 thinkingSignature"实现细节已变。③ **Mistral 分片合并**（#8387）：续片常无 `tool_call.id`，分片 key 改为 `toolCall.index ?? callId`（mistral-conversations.ts:567/:696），长参数 tool call 不再分裂。④ **OpenRouter reasoning controls 生成期推导**（#8614）：新 `scripts/openrouter-reasoning-options.ts` 从模型元数据（`mandatory`/`supported_efforts`/`default_effort`）生成 `thinkingLevelMap`（mandatory → `{off: null}` 禁止关闭）。⑤ 目录：`deepseek-v4-flash-vision-exp`（generate-models.ts:2571）、Cloudflare gateway 补 `workers-ai/*` passthrough、OpenRouter 图像模型刷新、glm-5.3 价格修正（非零价）。
>
> **✅ v0.85.1**（2026-09-10 核对）：本篇**几乎全部仍然有效**——三层架构、`lazyApi`/`lazyStream` 签名、`BUILTIN_APIS` 仍是 10 个、`api/` 仍是 32 个文件、Mistral 仍无 SDK 依赖，均未变。**只有一处失效**：`cloudflare-gateway-binding.ts` 已改名 `cloudflare-ai-binding.ts` 且语义反转（见下文"cross-provider 共享模块"）。另新增 `packages/ai` 的 `./utils/*` 子路径导出。

## 现象是什么

v0.83.0 中 `packages/ai/src/` 新增了 `api/` 目录，把原来混在 `providers/` 里的 wire-protocol 实现（怎么调 Anthropic Messages API、怎么调 OpenAI Responses API...）拆出来成为独立层。截至 v0.84.2 该目录有 **32 个文件**：11 个 API 实现 + 11 个 lazy wrapper + 10 个共享/基础设施文件。每个 stream API 有两个文件：一个实现（如 `anthropic-messages.ts`）+ 一个 lazy wrapper（如 `anthropic-messages.lazy.ts`）；openrouter-images 是图片生成 API，wrapper 直接返回 `ProviderImages`。同时 `auth/` 和 `compat/` 也从 providers 中拆出。

## 核心矛盾

**API adapter 的代码很重。** `bedrock-converse-stream.ts` ~42KB 带 AWS SDK 依赖，`openai-responses-shared.ts` 有复杂的 tool output 转换逻辑。如果启动时 eager-load 所有 adapter，即使只用 Anthropic，也要为 AWS SDK、Google SDK、OpenAI SDK 付出 import 成本（Mistral v0.84 起改走原生 `fetch`，不再拖 SDK）（硬事实）。

解法：**lazy loading 模式** — 从 `.lazy.ts` import 几乎是零成本的（一个闭包），真正的模块在第一次 `stream()` 调用时才加载（解释）。

## 实现思想

### 三层架构

```
providers/   →  "有什么模型"（Model catalog，纯数据）
api/         →  "怎么调 API"（Wire-protocol streaming）
auth/        →  "用什么凭证"（Credential resolution）
```

调用链：`Models.streamSimple()`（pi-ai 内接口；coding-agent 的 `ModelRuntime implements Models` 并委托给它）→ 按 model 的 `api` 字段 dispatch → 找到对应的 `ProviderStreams` → 调 `stream()` 或 `streamSimple()`。

### deferred response 契约（v0.84 新增）

`SimpleStreamOptions.deferred?: boolean | { window?: "15m" | "1h" | "24h" }` 请求 provider 返回 **durable handle** 而非即时响应（`StopReason` 新增 `"deferred"`）。之后用 `Models.fetchDeferred(model, handle, options)` 取回（long-poll `wait` 参数）、`Models.cancelDeferred(model, handle, options)` 取消——两者都走 `applyAuth()` 做 authenticated dispatch（硬事实）。`ProviderStreams` 上的 `fetchDeferred?/cancelDeferred?` 是可选方法；`createProvider()` 仅在任一 API 实现提供时才在 provider 上挂载；`lazyApi(load, { fetchDeferred, cancelDeferred })` 可以为 lazy wrapper 声明同样的 capability（硬事实）。内置 adapter 尚未实现该契约，`providers/faux.ts` 是参考实现/测试替身（推测）。

### lazyApi 和 lazyStream

`lazy.ts` 导出两个函数：

**`lazyApi(load, capabilities?): ProviderStreams`** — 接收 `() => import("./xxx.ts")`，返回一个 `{ stream, streamSimple }` 对象（v0.84 起可选 `{ fetchDeferred, cancelDeferred }` capabilities，为 deferred 方法包一层同样的 lazy dispatch）。首次调用任一方法时触发 `load()`，结果缓存在模块闭包中。后续调用直接走缓存。

**`lazyStream(model, setup): AssistantMessageEventStream`** — 更深的技巧：**同步返回**一个 `AssistantMessageEventStream`，而 `setup()` 在后台异步运行（硬事实）。如果 setup 失败（模块加载失败、auth 错误），错误编码为 stream 上的 error event，永不 throw（硬事实）。

这意味着调用方总是立即拿到 stream 对象——不需要 `await`。网络请求 + 模块加载并行进行（解释）。

### Stream 函数合约

每个 API 模块导出两个函数（能力强的还可以导出 deferred 方法），遵守相同的合约：

```ts
stream(model, context, options?) → AssistantMessageEventStream
streamSimple(model, context, options?) → AssistantMessageEventStream
fetchDeferred?(model, handle, options?) → AssistantMessageEventStream   // 可选
cancelDeferred?(model, handle, options?) → Promise<void>               // 可选
```

合约规则：
- **永不 throw**（硬事实）。所有错误（网络、auth、parse）编码为 stream event
- **成功**：`{ type: "done", reason: "stop" | "length" | "toolUse" | "deferred", message: AssistantMessage }` → stream 结束（硬事实）
- **失败**：`{ type: "error", reason: "aborted" | "error", error: AssistantMessage }` → `AssistantMessage` 带 `stopReason: "error"/"aborted"` 和 `errorMessage`（硬事实）

`stream` 接收 provider-specific 的 typed options（如 `OpenAIResponsesOptions` 有 `reasoningEffort`）。`streamSimple` 接收统一的 `SimpleStreamOptions`（有 `reasoning`、`thinkingBudgets`、`deferred`，v0.84.3 起还有 `toolChoice`），内部通过 `buildBaseOptions()` 自动 clamp maxTokens 到 context window 安全边界、合并 `model.samplingParams` 与请求级 `samplingParams`，然后调用 `stream`（硬事实）。

### 每个 `.lazy.ts` 的结构

所有 lazy wrapper 都是同一模式的一行代码：

```ts
// anthropic-messages.lazy.ts
export const anthropicMessagesApi = (): ProviderStreams => lazyApi(() => import("./anthropic-messages.ts"));
```

Bedrock 是个例外——它用变量字符串（非字面量）传 import path，防止 bundler 追踪到 Node-only 的 AWS SDK。同时暴露 `setBedrockProviderModule` 供 Bun binary 注入静态 bundle。

### cross-provider 共享模块

`transform-messages.ts` — 跨 provider 消息标准化：image downgrade、tool-call ID 重写、orphaned tool-call padding、加密 thinking signature 剥离。

`constrained-sampling.ts` — grammar-based 约束 JSON 采样 + JSON Schema strict mode 解析。工具可以声明 `constrainedSampling` 配置（`json_schema strict: prefer|require` 或 `grammar` variants），API adapter 据此启用 provider 的 structured output 能力。

`openai-responses-shared.ts` — OpenAI Responses 家族的共享转换逻辑（tool output → message、text signature 编码、caching helper、`additional_tools` deferred tool loading）。

`google-shared.ts` — Google Generative AI 与 Vertex 两个 adapter 的共享转换逻辑。

其余小共享件：`openai-prompt-cache.ts`（prompt cache key 64 字符截断）、`github-copilot-headers.ts`（`X-Initiator` / `Copilot-Vision-Request` header 推断）、`cloudflare.ts`（Workers AI / AI Gateway base URL 常量）、`cloudflare-ai-binding.ts`（**v0.85.1 改名 + 语义反转**：旧名 `cloudflare-gateway-binding.ts`，旧职责是"把 gateway HTTPS 请求翻译成 Workers AI binding 调用"；现在**不再翻译**——`createAiBindingFetch(binding)` 把请求原样透传给 `env.AI.fetch()`，方法、headers、query string、body stream 一律不改写不缓冲不重编码（文件头注释："Nothing is rewritten, buffered or re-encoded"），旧导出名 `createGatewayBindingFetch()` 同步改为 `createAiBindingFetch()`）。

另外 v0.85.1 给 `packages/ai` 的 `exports` 表新增了 **`./utils/*`** 子路径（`packages/ai/package.json`）——`utils/` 下的小共享件（如 `deferred-tools.ts`、`assistant-message-frame.ts`、`validation.ts`）从"仅包内可用"变成可被外部 import。

## 在 Agent 能力面里的影响

- **启动零成本**。import `@earendil-works/pi-ai` 不再加载 AWS SDK、Google SDK 等重型依赖。
- **添加新 provider** 只需：实现 `ProviderStreams` 合约 + 写一行 lazy wrapper + 在 `compat.ts` 的 `BUILTIN_APIS` 表中注册。
- **错误面统一**。所有 API 错误以 event 形式流入 Agent loop，不需要 per-provider try/catch。
- **`streamSimple` 是外部集成的最简路径**：只传 model + context + reasoning level，maxTokens 自动 clamp。

## 源码锚点

| 机制 | 路径 |
|------|------|
| lazy loading 引擎 | `packages/ai/src/api/lazy.ts` |
| Stream 合约 + `SimpleStreamOptions` | `packages/ai/src/api/simple-options.ts` |
| Anthropic Messages | `packages/ai/src/api/anthropic-messages.ts` + `.lazy.ts` |
| OpenAI Completions | `packages/ai/src/api/openai-completions.ts` + `.lazy.ts` |
| OpenAI Responses | `packages/ai/src/api/openai-responses.ts` + `.lazy.ts` |
| Mistral（原生 HTTP Chat Completions，无 SDK） | `packages/ai/src/api/mistral-conversations.ts` + `.lazy.ts` |
| Bedrock Converse | `packages/ai/src/api/bedrock-converse-stream.ts` + `.lazy.ts` |
| pi Messages (Radius) | `packages/ai/src/api/pi-messages.ts` + `.lazy.ts` |
| Cross-provider transform | `packages/ai/src/api/transform-messages.ts` |
| Constrained sampling | `packages/ai/src/api/constrained-sampling.ts` |
| Deferred tool loading | `packages/ai/src/utils/deferred-tools.ts`（`splitDeferredTools`） |
| API registry | `packages/ai/src/compat.ts` → `BUILTIN_APIS`（10 个 stream API；openrouter-images 由 `packages/ai/src/providers/openrouter-images.ts` 工厂装配） |
| Provider stream types | `packages/ai/src/types.ts` → `ProviderStreams`, `StreamFunction`, `DeferredHandle` |
| Deferred dispatch + faux provider | `packages/ai/src/models.ts`（`fetchDeferred`/`cancelDeferred`）、`packages/ai/src/providers/faux.ts` |

## 最小例子

直接使用一个 lazy API adapter：

```ts
import { anthropicMessagesApi } from "@earendil-works/pi-ai/api/anthropic-messages.lazy";

const api = anthropicMessagesApi(); // 零成本——还没加载 Anthropic SDK

const stream = api.streamSimple(model, context, {
  reasoning: "high",
  apiKey: "sk-...",
});
// stream 是同步返回的 AssistantMessageEventStream
// Anthropic SDK 在第一次 stream 调用时才 import

for await (const event of stream) {
  if (event.type === "text_delta") console.log(event.delta);
  if (event.type === "done") break;
}
```
