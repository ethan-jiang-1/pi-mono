# 2.7 Cancellation: 运行中的取消与信号传播

> **⚠️ v0.83.0**：`AgentEvent`→`AgentSessionEvent`；`agent_end`→`agent_settled`；abort signal 检查增强（`executeToolCallsSequential/Parallel` 中新增 `signal?.aborted` 检查）。行号全部失效。
>
> **⚠️ v0.84.2**：v0.84 无 cancellation breaking；全仓 auth/refresh 路径补齐 abort signal 传递（见 5.2）。行号未复核。

## 现象是什么

当用户按下 Ctrl+C 或点击 "Stop" 按钮时，pi-mono Agent 会优雅地中止当前运行：正在进行的 LLM 流式请求会被取消，正在执行的工具调用会被中断，Agent 状态回到 idle，并且 emit 一个携带 `stopReason: "aborted"` 的 `agent_end` 事件来表示此次运行的非正常终止。整个过程不会留下损坏的 Agent 状态。

**源码位置：**
- `/packages/agent/src/agent.ts`（`Agent.abort()`、`Agent._runWithLifecycle()`、`signal` 生命周期）
- `/packages/agent/src/agent-loop.ts`（signal 在 runLoop 中的传播）
- `/packages/agent/src/types.ts`（`AgentEvent` 中的 aborted 相关语义）

## 核心矛盾是什么

Agent 运行过程中有三个并发执行路径，取消信号需要传递到每一个：

1. **LLM 流式请求：** 一个正在等待服务端响应的 HTTP streaming 连接（在 provider 层）。
2. **并发工具调用：** 多个 `tool.execute()` 正在运行（可能是文件 I/O、shell 命令、网络请求等）。
3. **重试等待：** Agent 级重试中的一个 `sleep(delayMs)` 正在等待退避时间。

取消操作必须满足：
- 所有三个路径都能被中断（不能有"卡住"的取消）。
- 取消后的 Agent 状态必须干净（`isStreaming` 变为 false，`pendingToolCalls` 清空）。
- 取消后的事件协议必须完整（最终仍 emit `agent_end`）。

## 实现思想是什么

### AbortController 生命周期

`Agent` 在 `runWithLifecycle()` 中为每次 run 创建一个新的 `AbortController`：

```typescript
private async runWithLifecycle(executor: (signal: AbortSignal) => Promise<void>): Promise<void> {
  const abortController = new AbortController();
  let resolvePromise = () => {};
  const promise = new Promise<void>((resolve) => { resolvePromise = resolve; });
  this.activeRun = { promise, resolve: resolvePromise, abortController };

  this._state.isStreaming = true;
  try {
    await executor(abortController.signal);
  } catch (error) {
    await this.handleRunFailure(error, abortController.signal.aborted);
  } finally {
    this.finishRun(); // 清理 isStreaming, pendingToolCalls
  }
}
```

关键点：
- `AbortController` 在 executor 开始前创建，在整个 run 过程中存活。
- `signal` 通过 `executor` → `runAgentLoop` → `streamSimple(options: { signal })` 的路径传递给 provider。
- `signal` 也传递给 `tool.execute(toolCallId, params, signal)`，工具实现负责检查 `signal.aborted`。
- `runWithLifecycle` 包装 executor 的异常：如果运行正常完成或抛出异常，都走 `finishRun()` 清理。

### Agent.abort()

```typescript
abort(): void {
  this.activeRun?.abortController.abort();
}
```

调用后，`abortController.signal.aborted` 变为 `true`，所有持有此 signal 的操作都会感知到。

### 在 runLoop 中的取消处理

在 `streamAssistantResponse()` 中，如果响应流的最终结果是 `stopReason === "aborted"`，loop 立即终止：

```typescript
if (message.stopReason === "error" || message.stopReason === "aborted") {
  await emit({ type: "turn_end", message, toolResults: [] });
  await emit({ type: "agent_end", messages: newMessages });
  return;
}
```

这意味着如果 abort 发生在 LLM 调用期间，当前 turn 仍然完整 emit `turn_end` 和 `agent_end`，不会有挂起的 turn。

### 错误表面

当 abort 发生时：

1. Provider 层的 HTTP 请求被取消，SDK 抛出 `AbortError`。
2. Provider 将此转换为 `stopReason: "aborted"` 的 AssistantMessage，通过 `error` 事件在流中发出。
3. `streamAssistantResponse()` 的 `done`/`error` case 捕获此消息。
4. Loop 检查 `stopReason === "aborted"` 并提前退出。
5. 如果 abort 发生在 `runWithLifecycle` 的 executor 抛异常时（而非 stream 内部处理），`handleRunFailure` 创建一个合成的助手消息（`stopReason: "aborted"`），emit message_start/end、turn_end、agent_end。

两种路径最终都产生完整的 agent_end 事件，只是 aborted 消息的内容不同（一个是 provider 返回的真实错误消息，一个是合成的空消息）。

### 扩展的 abort 支持

扩展钩子（`beforeToolCall`、`afterToolCall` 等）接收 `AbortSignal` 参数，可以传播给内部的异步操作。此外，扩展可以通过 `AgentHarness` 的 `abort()` 方法触发取消。

## 在 Agent 能力面里的影响是什么

- **优雅取消体验：** 取消不会导致 Agent 状态损坏。用户可以立即在取消后发送新 prompt，Agent 状态干净。
- **事件协议的完整性：** 即使是取消，`agent_end` 事件仍会 emit，UI 层可以可靠地依赖这个事件来处理渲染清理和状态重置。
- **工具执行的中断：** 工具实现如果检查 `signal.aborted`，可以在取消时优雅地关闭文件句柄、终止子进程、回滚部分操作。
- **重试取消：** 如果取消发生在退避等待期间，`_retryAbortController.abort()` 终止 sleep，emit `auto_retry_end(success: false)`。

## 源码锚点是什么

| 符号 | 文件 | 行号 | 角色 |
|------|------|------|------|
| `Agent.abort()` | agent.ts | 297-299 | 触发取消 |
| `Agent.signal` | agent.ts | 292-294 | 当前 activeRun 的 AbortSignal |
| `Agent._runWithLifecycle()` | agent.ts | 448-471 | AbortController 创建和生命周期 |
| `Agent.handleRunFailure()` | agent.ts | 473-489 | abort 时的合成错误消息 |
| `Agent.finishRun()` | agent.ts | 491-497 | 清理运行时状态 |
| `ActiveRun.abortController` | agent.ts | 151-155 | ActiveRun 类型定义 |
| `runLoop()` signal 传播 | agent-loop.ts | 155 | signal 从 config 传入 runLoop |
| `streamAssistantResponse()` abort 处理 | agent-loop.ts | 196-200 | stopReason check 提前退出 |
| `executePreparedToolCall()` signal | agent-loop.ts | 604 | signal 传给 tool.execute() |
| `streamSimple()` 接收 signal | stream.ts | 43 | signal 在 stream options 中传递 |
| `AgentSession.abortRetry()` | agent-session.ts | 2533-2537 | 取消重试退避等待 |

## 最小例子是什么

```typescript
import { Agent } from "@earendil-works/pi-agent";

const agent = new Agent({
  initialModel: claudeSonnet,
  tools: [
    {
      name: "long_running_task",
      label: "Long Running Task",
      description: "A task that takes a long time",
      parameters: { type: "object", properties: {} },
      execute: async (id, params, signal) => {
        // 工具实现检查 signal 以支持取消
        for (let i = 0; i < 100; i++) {
          if (signal?.aborted) {
            throw new Error("Task cancelled");
          }
          await new Promise(resolve => setTimeout(resolve, 1000));
        }
        return { content: [{ type: "text", text: "Done!" }], details: {} };
      },
    },
  ],
  systemPrompt: "You are a task runner.",
});

// 监听事件，即使是 abort 也能看到完整的生命周期
agent.subscribe((event) => {
  switch (event.type) {
    case "agent_start":
      console.log("Agent 启动");
      break;
    case "agent_end":
      console.log(`Agent 结束 (共 ${event.messages.length} 条消息)`);
      const lastMsg = event.messages[event.messages.length - 1];
      if (lastMsg.role === "assistant" && lastMsg.stopReason === "aborted") {
        console.log("--- 运行被取消 ---");
      }
      break;
  }
});

// 启动运行
const promptPromise = agent.prompt("Run the long running task");

// 2 秒后取消
setTimeout(() => {
  console.log("取消中...");
  agent.abort();
}, 2000);

await promptPromise;

console.log("Agent isStreaming:", agent.state.isStreaming); // false
console.log("Agent pendingToolCalls:", agent.state.pendingToolCalls.size); // 0
// Agent 状态干净，可以立即发新 prompt
await agent.prompt("Try a different task");
```

这个例子展示了完整的取消生命周期：signal 从 Agent 传播到 tool → tool 检查 `signal.aborted` → stream 以 `stopReason: "aborted"` 结束 → agent_end 事件仍正常 emit → 状态清理干净，可以立即开始新运行。
