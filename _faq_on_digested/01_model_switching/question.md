# 问题

pi-mono 的 TUI 里怎么换模型？我知道有 Ctrl+P 可以切模型，但：

1. Ctrl+P 切换的模型列表是哪来的？怎么控制哪些模型参与轮换？
2. 怎么直接选一个特定模型（不通过轮换）？
3. thinking level 又是怎么切的？它和模型切换是什么关系？
4. 这些快捷键背后调的是 AgentSession 的哪些方法？如果不用 TUI（走 SDK/RPC），怎么等价地做？
