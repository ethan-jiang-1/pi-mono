# 同步 0002：v0.75.3 → v0.83.0（粗糙）

**日期**: 2026-07-30
**upstream 范围**: `76705633` → `71efc6f0`
**提交数量**: 989
**发布标签**: v0.75.4, v0.75.5, v0.76.0, v0.77.0, v0.78.0, v0.78.1, v0.79.0–v0.79.10, v0.80.0–v0.80.10, v0.81.0–v0.81.1, v0.82.0–v0.82.1, v0.83.0
**时间跨度**: 2026-05-18 → 2026-07-30（~2.5 个月）

## 核心主题

这是一次 **2.5 个月、8 个 minor 版本、989 个 commit** 的巨大 gap。整体感受：pi-mono 在快速迭代——新增了大量模型支持、重做了 AI provider 架构、扩展系统大幅升级、agent harness 新增 tools 层。

## 按包拆解

### packages/ai（386 commits）

- **目录重构**：新增 `api/`、`auth/`、`compat/` 子目录；OAuth 从 `utils/oauth` 迁移到 `auth/oauth`
- **OAuth 支持**：新增 OpenRouter OAuth、GitHub Copilot OAuth、Kimi Code subscription OAuth
- **constrained sampling**：新增约束采样能力
- **新模型**：Claude Opus 5（含 Bedrock）、新 Gemini 模型、generated image models
- **模型目录**：model types 改为从 JSON 派生生成；支持 ETag revalidation
- **流式改进**：支持 pending stop reason while streaming
- **per-request fetch injection**

### packages/agent（153 commits）

- **新目录**：`harness/tools/` — agent harness 新增工具执行层
- **agent-harness 改版**：14 commits 改动 `harness/types.ts`，16 commits 改动 `compaction.ts`
- **agent-loop**: 13 commits 改动
- **types.ts**: 9 commits 改动 — API surface 可能有重要变化

### packages/coding-agent（532 commits，变化最大）

- **新目录**：`extensions/`、`extensions/llama/` — 扩展系统移到顶层，新增 llama 扩展
- **agent-session.ts**: 54 commits（最热的文件）
- **extensions/types.ts**: 27 commits
- **extensions/runner.ts**: 19 commits
- **session-manager.ts**: 18 commits
- **sdk.ts**: 16 commits
- **新能力**：bash_execution_update 事件、session metadata 暴露给 bash tools、output padding for custom renderers、vitest eval harness

### packages/tui（149 commits）

- 此前 _digested/ 未覆盖 tui，但 149 commits 表明终端 UI 在持续演进
- 新增 debug/crash log 写入 pi agent dir

## 对 `_digested/` 的影响评估

| 子目录 | 影响 | 说明 |
|--------|------|------|
| **agent/** | 🔴 严重 | 几乎所有引用的源文件都有大量改动。`agent-loop.ts`(13)、`harness/types.ts`(14)、`harness/agent-harness.ts`(13)、`compaction.ts`(16)——行号全部失效，机制描述可能过时。新目录 `harness/tools/` 完全未被覆盖。 |
| **integration/** | 🔴 严重 | `agent-session.ts`(54 commits)、`sdk.ts`(16)、`extensions/types.ts`(27)、`rpc-types.ts`(4)——SDK/RPC API surface 可能有重要变化。coverage-and-parity 表需要重审。 |
| _change_log/ | 🟢 新建 | 本文件即第一条 sync record |

### 关键文件的 commit 数（列举变化最大的）

```
54  packages/coding-agent/src/core/agent-session.ts
42  packages/ai/src/types.ts
27  packages/coding-agent/src/core/extensions/types.ts
19  packages/coding-agent/src/core/extensions/runner.ts
18  packages/coding-agent/src/core/session-manager.ts
16  packages/coding-agent/src/core/sdk.ts
16  packages/agent/src/harness/compaction/compaction.ts
14  packages/agent/src/harness/types.ts
13  packages/agent/src/agent-loop.ts
13  packages/agent/src/harness/agent-harness.ts
 9  packages/agent/src/types.ts
 7  packages/agent/src/agent.ts
```

### 已知新增但 _digested/ 未覆盖的结构

- `packages/agent/src/harness/tools/` — 新目录
- `packages/coding-agent/src/extensions/`、`extensions/llama/` — 新目录
- `packages/ai/src/api/`、`auth/`、`auth/oauth/`、`compat/` — 目录重构

## 优先级建议

1. **先不要逐篇修**。gap 太大，逐篇修会花很长时间且容易漏。
2. **先读 diff 建立主题清单**：按主题（agent harness tools、extensions 重构、AI auth、constrained sampling 等）分组读 diff，写一条 `_plan-1-v0.83.0.md` 列出需要更新的文档和新增专题的优先级。
3. **先修 integration/**：SDK/RPC 入口是外部集成最关心的，且 `agent-session.ts`(54 commits) 改动最大，API 可能已有重要变化。
4. **再修 agent/**：行号全部漂移，但核心机制（loop、compaction、harness）大概率思想没变——需要验证后再决定重写还是微调。
5. **最后考虑新增专题**：harness/tools、extensions/llama、AI auth/OAuth 如果对理解 pi-mono 外部集成重要，可以开新篇。

## 验收基线

当前 upstream main 的状态：

```
git describe --tags upstream/main  → v0.83.0
git rev-parse upstream/main       → 71efc6f0...
```
